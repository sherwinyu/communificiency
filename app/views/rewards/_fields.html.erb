<% f.object.class.columns.each do |rewards_column| %>
  <%= f.label "#{rewards_column.name}".to_sym %> 
  <%= generic_form_field(f.object, f, rewards_column) %>
  <br>
<% end %>
  <%= f.label :_destroy, "Remove question" %>
  <%= f.check_box :_destroy %>
