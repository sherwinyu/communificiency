
<div class="container">
  <div class="row">
    <div class="span8">
      <%= render partial: 'layouts/flash', locals: {:flash => flash}%>
      <div class="hero-unit">
        <div class="group">
          <%# image_tag(@project.picture, alt: @project.name, class: "project-banner") %>
          <%= image_tag(@project.picture, alt: @project.name, class: "project-banner") if @project.picture and find_asset @project.picture %>
          <div class="black-mask project-banner"></div>
          <h1 class="overlay" id="project-banner"><%# @project.name %></h1>
        </div>


        <%#==========================MAIN TEXT============================%>
        <h3 class="content-header">The Project</h3>
        <p>
        <div class="markdown"><%= markdown @project.short_description %></div>

        <div class="markdown"> <%= markdown(@project.long_description) %> </div>
      </div> <!-- /hero unit -->


    </div><!--/span-->

    <%#==========================REWARD BAR============================%>


    <div class="span4 rewards-bar">
      <div class="rewards-container ">
        <h3 class="reward-header"> Contribution levels </h3>
        <div class="contribution-info well">
          <h4>25% funded</h4>
          <h4>$350 of $1,400 raised</h4>
          <h4>281 contributors</h4>
        </div>
        <div class='well'>
          <%= simple_form_for(@contribution, url: new_project_contribution_path(@project), method: 'GET') do |f| %>

            <%= f.input :amount, wrapper: 'prepend' do %>
              <%= content_tag :span, "$", class: "add-on" %>
              <%= f.input_field :amount, class: 'span2' %>
              <%= content_tag :span, ".00", class: "add-on" %>
            <% end %>
          <% end %>
        </div>
        <% @project.rewards.each do |reward| %>
          <div class="reward">
            <h3 class="reward-tag"> Pledge $<%= reward.minimum_contribution %> </h3>
            <%= link_to 'Contribute', new_project_contribution_path(@project, reward_id: reward),  class: "btn reward-btn" %>
            <p> <%= reward.long_description %> </p>
          </div>
        <% end %>

      </div>


    </div><!--/span-->
  </div><!--/row-->

  <hr>


</div><!--/.fluid-container-->



<%= link_to 'Edit', edit_project_path(@project) if current_user && current_user.admin? %> |
<%= link_to 'Back', projects_path %>
